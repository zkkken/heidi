# ============================================
# EMR to Heidi Integration - 更新版依赖
# Python 3.10+ (推荐 3.10-3.12，当前 3.14.0)
# ============================================

# -------------------- 核心依赖（自动选择最新兼容版本）--------------------

# 截图相关
pyautogui>=0.9.54

# 图像处理
Pillow>=10.0.0

# OCR 引擎（关键：不固定版本，让 pip 自动选择）
paddleocr>=2.7.0

# HTTP 请求
requests>=2.31.0

# 环境变量管理
python-dotenv>=1.0.0

# 终端美化输出
rich>=13.0.0

# 配置文件解析
pyyaml>=6.0

# -------------------- 可选依赖 --------------------

# 多屏幕支持
screeninfo>=0.8

# -------------------- 说明 --------------------
# 1. 使用 >= 而不是 ==，让 pip 选择最新兼容版本
# 2. paddlepaddle 会作为 paddleocr 的依赖自动安装
# 3. pymupdf (muPDF) 不是必需的，会自动跳过
